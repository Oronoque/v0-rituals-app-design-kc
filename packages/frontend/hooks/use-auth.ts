"use client";

// Re-export the new React Query auth hooks for compatibility
export { useAuth } from "./use-api";

// Note: This file now serves as a compatibility layer.
// All new code should import directly from './use-api' for the full set of hooks.
