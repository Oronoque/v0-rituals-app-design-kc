{
  "name": "@rituals/shared",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "bun run build:tsc",
    "build:tsc": "tsc",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "async_hooks": "^1.0.0",
    "express": "^5.1.0",
    "kysely": "^0.27.0",
    "neverthrow": "^8.2.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5"
  }
}
